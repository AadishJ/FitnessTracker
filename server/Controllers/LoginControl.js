async function handleLoginGet ( req, res )
{
    return res.status(200);
}
async function handleLoginPost ( req, res )
{
    return res.status( 200 );
}

module.exports = {
    handleLoginGet,
    handleLoginPost
}